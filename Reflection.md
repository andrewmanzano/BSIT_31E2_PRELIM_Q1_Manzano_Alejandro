Inheritance was used to share core properties and logic from a single parent class to child classes
Interfaces were used to define common behaviors without dictating how a class must execute them.
No, because an object can only have one true ancestry line to prevent conflicting instructions.
helicopter can implement multiple interfaces because interfaces only list method signatures without containing any actual code.
just add a new interface for dive and sail.
